import { Box, styled } from "@mui/material";

export const SpinnerContainer = styled(Box)(() => ({
    height: "80vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
}));
